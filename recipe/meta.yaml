{% set name = "scitacean" %}
{% set version = "23.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/scitacean-{{ version }}.tar.gz
  sha256: 4a31fca78c2afa8657aacf62bdd16f01d0665323c1c6f6e6d0c6a56ae7194303

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - email-validator
    - fabric
    - pydantic
    - python-dateutil
    - requests

test:
  imports:
    - scitacean
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/scitacean/
  summary: High-level interface for SciCat
  dev_url: https://github.com/SciCatProject/scitacean
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jl-wynen
    - nitrosx
